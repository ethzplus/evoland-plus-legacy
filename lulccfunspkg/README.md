# README

This package is set up to avoid namespace issues while making the LULCC-CH code testable. It is intended that this package grows as I refactor the R code in `../scripts/`.

The initial batch of files has been inherited from <https://github.com/blenback/LULCC-CH-private/tree/private-remote/Scripts/Functions>.
